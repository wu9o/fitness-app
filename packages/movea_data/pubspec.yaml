name: movea_data
description: Local-first stores and platform ports for Movea.
publish_to: none
version: 0.1.0
environment:
  sdk: '>=3.3.0 <4.0.0'
dependencies:
  crypto: ^3.0.7
  cryptography: ^2.9.0
  flutter:
    sdk: flutter
  movea_domain:
    path: ../movea_domain
  shared_preferences: ^2.5.3
